{
	"name": "p",
	"description": "Static site for Portfolio",
	"version": "0.0.1",
	"type": "module",
	"scripts": {
		"dev": "yarn svelte:dev",
		"build": "yarn svelte:build",
		"new": "cp $INIT_CWD/src/work/template $INIT_CWD/src/work/$(date +%F)-${0}.md && echo \"Created $INIT_CWD/src/work/$(date +%F)-${0}.md\"",
		"preview": "yarn svelte:preview"
	},
	"devDependencies": {
		"@types/headroom.js": "^0.12.4",
		"eslint-config": "workspace:^",
		"svelte-config": "workspace:^",
		"typescript": "^5.8.2"
	},
	"dependencies": {
		"headroom.js": "^0.12.0"
	}
}
